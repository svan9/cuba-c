[!#joke]
yas std;

entry main();

[!#gen]
xat list<T> {
  "count of elements (unsigned integer)";
  `iew size;

  "size of element";
  `iew size_t = sizeof(T);
  
  "start pointer";
  $ address;

  on {
    [`iew i] -> {
      rnd ((self.address*self->size_t)/10);
    };
  };
};

[!#flags_support, !#bii]
xat tom {
  list<$> arguments;
  block inner;

  "translate root object prems";
  self~inner;
  
  on {
    () -> {
      rnd (self::call);
    };
  };
  
};


iew main() {
  iew x1 = 10; 
  iew x2 = 15; 
  
  "condition";
  iiow (x1 >= x2) {

  }
  "if not touched"; 
  edie {
    
  } 
  "if touched";
  iiod {

  }
};

xat foo {
  [!#festo]
  tom create;
};

tom foo::create() {
  late tom so_tom = tom::new() {};
  printout BUFFER;
  writefile BUFFER;
  BUFFER buf = readfile PATHLIKE;
  "BUFFER == string";

  BUFFER buf;
  BUFFER nbuf = execute buf;
  end;
};

